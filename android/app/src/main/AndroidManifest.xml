<?xml version="1.0" encoding="utf-8"?>
<!--
  Reference manifest. In your Android Studio project, the generated manifest
  already has <application> with MainActivity — just MERGE IN the two highlighted
  pieces: the INTERNET permission, and the <service> block. The service MUST
  require BIND_SCREENING_SERVICE and declare the CallScreeningService intent
  filter, or Android won't offer the role.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- MERGE IN: needed to sync the blocklist -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MERGE IN: the call-screening service -->
        <service
            android:name=".CallScreeningServiceImpl"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>
    </application>
</manifest>
